---
name: spec-fail-missing-required-fields
description: Validate required frontmatter coverage for license and metadata. Use when checking whether mandatory metadata keys are enforced.
metadata:
  author: qa-team
---

# Missing Required Fields

## Workflow

1. Run the checker on this fixture.
2. Confirm required frontmatter findings are emitted.
3. Verify the result is marked as fail.
