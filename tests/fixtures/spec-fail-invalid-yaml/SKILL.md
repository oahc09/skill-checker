---
name: spec-fail-invalid-yaml
description: Check malformed YAML. Use when an agent needs to catch parse errors in a skill file.
metadata:
  owner:
---

# Invalid YAML

This body is present but the frontmatter is malformed.
